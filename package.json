{
  "name": "plock",
  "version": "1.4.5",
  "description": "Promise-based locking",
  "source": "src/index.mjs",
  "module": "src/index.mjs",
  "main": "src/index.mjs",
  "scripts": {
    "clean": "rm -rf .nyc_output/ coverage/ node_modules/.cache/",
    "format": "prettier-standard {src,test}/*.{m,}js",
    "lint": "standard {src,test}/*.{m,}js|snazzy",
    "test": "nyc ava",
    "coverage": "npm run test && nyc report --reporter=html && sirv coverage/ -p 8000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ava": "^3.15.0",
    "esm": "^3.2.25",
    "promise-goodies": "github:ludlovian/promise-goodies"
  },
  "ava": {
    "require": [
      "esm"
    ]
  }
}
